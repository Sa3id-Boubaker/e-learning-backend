apiVersion: v1
kind: Service
metadata:
  name: course-service
  namespace: omarise
  labels:
    app: course-service
spec:
  type: ClusterIP
  selector:
    app: course-service
  ports:
    - name: http
      port: 8082
      targetPort: 8082
