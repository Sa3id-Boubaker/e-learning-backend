apiVersion: v1
kind: Service
metadata:
  name: forum-service
  namespace: omarise
  labels:
    app: forum-service
spec:
  type: ClusterIP
  selector:
    app: forum-service
  ports:
    - name: http
      port: 8085
      targetPort: 8085
