# EXAMPLE ONLY — do not apply as-is, do not commit real values.
# Consumed by: user-service, course-service, training-service, forum-service,
# notification-service (spring.data.mongodb.uri = mongodb+srv://${MONGO_DB_USER}:${MONGO_DB_PASSWORD}@...).
# eureka-server and api-gateway do not use MongoDB.
apiVersion: v1
kind: Secret
metadata:
  name: omarise-mongo
  namespace: omarise
type: Opaque
stringData:
  MONGO_DB_USER: CHANGE_ME
  MONGO_DB_PASSWORD: CHANGE_ME
