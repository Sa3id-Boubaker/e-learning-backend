# EXAMPLE ONLY — do not apply as-is, do not commit real values.
# Consumed by: user-service, course-service, training-service, forum-service,
# notification-service (jwt.secret). eureka-server and api-gateway do not sign/verify JWTs.
apiVersion: v1
kind: Secret
metadata:
  name: omarise-jwt
  namespace: omarise
type: Opaque
stringData:
  JWT_SECRET: CHANGE_ME
