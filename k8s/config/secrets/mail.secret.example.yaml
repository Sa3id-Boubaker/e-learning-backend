# EXAMPLE ONLY — do not apply as-is, do not commit real values.
# Consumed by: user-service only (spring.mail.username/password).
apiVersion: v1
kind: Secret
metadata:
  name: omarise-mail
  namespace: omarise
type: Opaque
stringData:
  MAIL_USERNAME: CHANGE_ME
  MAIL_PASSWORD: CHANGE_ME
